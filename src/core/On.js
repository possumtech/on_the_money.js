export default class On {
  static on(parent, event, selector, fn) {
    throw new Error('On.on: Not implemented');
  }

  static emit(el, event, detail) {
    throw new Error('On.emit: Not implemented');
  }
}
