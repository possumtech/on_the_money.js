{
  "name": "on_the_money.js",
  "version": "0.1.1",
  "description": "An opinionated web project anti-framework enforcing deterministic, DOM-first architecture.",
  "type": "module",
  "main": "src/core/index.js",
  "bin": {
    "otm-lint": "./src/linter/cli.js"
  },
  "scripts": {
    "test": "node --test test/**/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-lines=100 --test-coverage-branches=100 --test-coverage-functions=100 test/**/*.test.js",
    "lint": "node src/linter/cli.js --check ./src"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "espree": "^10.0.0",
    "parse5": "^7.0.0",
    "css-tree": "^3.0.0"
  },
  "license": "MIT"
}
